[package]
name = "alpha-core"
version = "0.1.0"
edition = "2021"

[dependencies]
blake3 = "1"
rand = "0.8"
rand_chacha = "0.3"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
proptest = "1"
