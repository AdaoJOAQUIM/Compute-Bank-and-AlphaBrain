[package]
name = "alpha-control"
version = "0.1.0"
edition = "2021"

[dependencies]
alpha-core = { path = "../alpha-core" }
alpha-sim = { path = "../alpha-sim" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.8"
